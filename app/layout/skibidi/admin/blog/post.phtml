<?php
$post = $this('post')
?>
<div class="admin-header">
    <h1 class="admin-title">Blog Post</h1>
    <a class="btn btn-primary" onclick="history.back()">Back</a>
</div>

<div class="form-container">
    <div class="form-card">

        <form method="post" action="/admin/blog/<?= $post['id'] ? 'update' : 'put' ?> " enctype="multipart/form-data" class="blog-post-form">
            <input type="hidden" name="post[id]" value=<?= $post['id'] ?>>
            <div class="form-group">
                <label for="post-title">Post Title</label>
                <input value="<?= $this('post')['title'] ?> " type="text" id="post-title" name="post[title]" placeholder="Enter your post title" required>
            </div>

            <div class="form-group">
                <label for="post-image">Featured Image</label>
                <?php if ($this('post')['image']):?>
                    <img height="100px" src="/<?= htmlspecialchars(trim($this('post')['image'], '/')) ?>" alt="<?= htmlspecialchars($post['title']) ?>" class="blog-card-image">
                <?php endif ?>
                <div class="file-upload-wrapper">
                    <input type="file" id="post-image" name="post_image[]" accept="image/*" class="file-input">
                    <div class="file-upload-area">
                        <div class="upload-icon">📁</div>
                        <p class="upload-text">Click to upload or drag and drop</p>
                        <p class="upload-subtext">PNG, JPG, GIF up to 5MB</p>
                    </div>
                    <div class="file-preview hidden">
                        <img class="preview-image" src="" alt="Preview">
                        <button type="button" class="remove-file">×</button>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label for="post-content">Content</label>
                <textarea id="post-content" name="post[content]" class="summernote-editor" placeholder="Write your post content here..."><?=htmlspecialchars($post['content'] ?? '', ENT_QUOTES, 'UTF-8')?></textarea>
            </div>

            <button type="submit" class="submit-btn">
                Save
            </button>
        </form>
    </div>
</div>
